import React from 'react';
import './Loading.css'

const Loading = () => {
    return (
        <div className="loading-overlay">
             <div style={{fontFamily:'Lobster'}} className="loading-text">SWIGO</div>
        </div>
    );
};

export default Loading;